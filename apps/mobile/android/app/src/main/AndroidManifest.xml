<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ⛔ THE RELEASE BUILD HAD NO NETWORK AT ALL WITHOUT THIS.

         Flutter writes `INTERNET` into the generated `debug/` and `profile/`
         manifests for its own tooling, and into neither `main/` nor `release/`.
         So every debug build could reach the server and every RELEASE build
         could not — DNS failed inside the process before a single request left
         it.

         It hid for as long as it did because a release build on a phone with a
         warm 60-day local store still renders every screen: Today, sleep,
         recovery, biological age, all of it, from cache. Only the writes were
         gone. The visible symptom was a push that "could not be reached" and a
         sign-in reporting that the host name "could not be looked up" — both
         accurate, and both read as network or server faults for hours.

         Diagnosed 2026-09-09 by `dumpsys package` listing ACCESS_NETWORK_STATE
         and no INTERNET, while `adb shell curl` to the same host succeeded —
         shell runs as the shell user, the app does not. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Bluetooth LE, for the strap. Scanning is all this app does with the
         radio at pairing time; the protocol layer lands separately.

         `neverForLocation` is the load-bearing attribute: it tells Android this
         scan is not being used to derive the user's position, which is why
         Android 12+ never prompts for location here. The pairing screen tells
         the owner exactly that, so the flag is what keeps the sentence true.

         Android 11 and below have no BLUETOOTH_SCAN and gate BLE scanning on
         ACCESS_FINE_LOCATION instead, so location permissions are capped at
         API 30. This app does not record GPS routes. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- required=false so the app still installs on a device without BLE. It
         cannot sync there, and `BluetoothUnavailable` says so in as many words
         rather than the Play Store silently hiding the listing. -->
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <application
        android:label="healthee"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ hides installed apps from us unless we declare what we
             intend to open. Without these two, `launchUrl` resolves nothing and
             the support buttons fail silently on exactly the phones most likely
             to have a UPI app installed. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="upi"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
